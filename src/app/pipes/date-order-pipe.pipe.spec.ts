import { DateOrderPipePipe } from './date-order-pipe.pipe';

describe('DateOrderPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DateOrderPipePipe();
    expect(pipe).toBeTruthy();
  });
});
